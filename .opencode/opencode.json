{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "plan": {
      "description": "Create an implementation plan",
      "template": "Use the shared commands/plan.md workflow and shared agents/skills."
    },
    "verify": {
      "description": "Verify implementation quality",
      "template": "Use the shared commands/verify.md workflow and relevant QA/review agents."
    },
    "engine-review": {
      "description": "Review one engine layer",
      "template": "Pick one engine layer only and use the matching review command."
    }
  }
}