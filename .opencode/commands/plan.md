# /plan

Resolve to the shared `commands/plan.md` workflow.

## Adapter behavior
- use `planner` as primary owner
- pull from `skills/workflow/vertical-slice-planning` or `skills/workflow/milestone-planning` as needed
- keep the output engine-neutral unless the request is explicitly engine-specific
