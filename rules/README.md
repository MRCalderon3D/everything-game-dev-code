# Rules

Layers:
- `common/` -> universal for game development
- `unity/` -> Unity-only override
- `unreal/` -> Unreal-only override
- `godot/` -> Godot-only override

Rules say what to do.
Skills explain how to do it.
